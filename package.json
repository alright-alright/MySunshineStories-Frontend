{
  "name": "mysunshinestories-frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",
    "start": "vite preview --host 0.0.0.0 --port $PORT",
    "preview": "vite preview --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "react": "^18.0.0",
    "vite": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
